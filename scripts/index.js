/* global store, Item, cuid, shoppingList*/

// eslint-disable-next-line no-unused-vars

'use strict';


$(document).ready(function() {
  shoppingList.bindEventListeners();
  shoppingList.render();
});

console.log(store);
console.log(Item);
console.log(foo);